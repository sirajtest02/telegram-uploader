<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.siraj.telegramuploader">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TelegramUploader">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Video Vault Activity -->
        <activity
            android:name=".VideoVaultActivity"
            android:exported="false" />

        <!-- Photo Vault Activity -->
        <activity
            android:name=".PhotoVaultActivity"
            android:exported="false" />

        <!-- Secret Chat Activity -->
        <activity
            android:name=".SecretChatActivity"
            android:exported="false" />

        <!-- Full Screen Image Activity -->
        <activity
            android:name=".FullScreenImageActivity"
            android:exported="false" />

    </application>

</manifest>
